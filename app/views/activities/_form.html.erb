<div class="form-container">
  <%= simple_form_for [@event, @activity] do |form| %>
    <%= form.error_notification %>
    <%= form.input :name %>
    <%= form.input :description, as: :text %>
    <%= form.input :duration %>
    <%= form.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>
