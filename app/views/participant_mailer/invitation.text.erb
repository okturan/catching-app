Invitation from <%= mail_safe(@organizer.name) %> (<%= @organizer.email %>)

<%= mail_safe(@event.name) %>
<% if @offer %><%= @offer %>
<% end %>
Paint the times that work for you:

<%= @link %>

You will get at most: up to 5 resends, one confirmation when you reply, and one message when the time is set.

If you don't know this person, ignore this email. You can also leave the event from the page above.
