<%= mail_safe(@organizer&.name) %> cancelled <%= mail_safe(@event.name) %>.
<% if @recipient_window %>
It was set for <%= @recipient_window %>.
<% if @recipient_window != @event_window %>
In the event's zone: <%= @event_window %>
<% end %>
<% end %>
Nothing else will be sent about this event.
<% if @recipient_window %>
If you added it to your calendar, the attached file removes it.
<% end %>
