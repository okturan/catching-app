#!/bin/bash -e

# Create or migrate the database immediately before starting the Rails server.
if [ "${@: -2:1}" = "./bin/rails" ] && [ "${@: -1:1}" = "server" ]; then
  ./bin/rails db:prepare
fi

exec "$@"
